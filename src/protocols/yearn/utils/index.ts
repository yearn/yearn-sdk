import * as vault from "../assets/vault";
import * as vaults from "../assets/vaults";
export default { vault, vaults };

// yearn.assets.deposits.all()
// yearn.assets.lent.all()
// yearn.assets.staked.all()
// yearn.assets.all()

// need way to
// .get all vaults
// .get all user vaults
// .get all earn
// .get all user earn
// .get all gov

// sdk.accounts(['0x123']).protocols.yearn.assets.allocation
// sdk.accounts(['0x123']).protocols.yearn.assets.deposits
// sdk.accounts(['0x123']).protocols.yearn.tvl

// sdk.accounts(['0x123']).protocols.all.assets.allocation
// sdk.accounts(['0x123']).protocols.all.assets.deposits
// sdk.accounts(['0x123']).protocols.all.tvl

// asset.apy
// sdk.protocols.all.allocation
// sdk.protocols.all.deposits
